services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ungokonom
      POSTGRES_PASSWORD: ungokonom
      POSTGRES_DB: ungokonom
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
